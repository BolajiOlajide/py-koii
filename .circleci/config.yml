version: 2 # use CircleCI 2.0
jobs: # A basic unit of work in a run
  # using tox
  toxify:
      docker:
        - image: python:3.6
      steps:
        - checkout
        - run: pip install -r requirements.txt && tox


workflows:
  version: 2

  myproj:
    jobs:
      - toxify
